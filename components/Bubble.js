import styles from './Navbar.module.css'

export default function Bubble(){
         const motion=document.querySelector('styles.line');
         alert(motion);
          hamburger = document.querySelector('styles.hamburger');
          navlinks = document.querySelector('styles.navlinks');
      
          navlinks.classList.toggle('open');
             
         motion.forEach(bar => {
                    bar.classList.toggle('animate')
                });
         console.log(motion)
              
          };
          